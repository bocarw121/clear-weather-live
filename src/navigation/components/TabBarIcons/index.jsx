import React from "react";
import Icon from "react-native-vector-icons/MaterialCommunityIcons";

const TabBarIcons = ({ name, color, size }) => {
  return <Icon name={name} color={color} size={size} />;
};

export default TabBarIcons;
